{
  "name": "pinoleros-ai",
  "version": "1.0.0",
  "description": "PINOLEROS AI Cloudflare Worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run deploy:dry-run"
  },
  "keywords": [
    "cloudflare-workers",
    "typescript"
  ],
  "author": "Wil-fandrade",
  "license": "ISC",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.131.1"
  }
}
